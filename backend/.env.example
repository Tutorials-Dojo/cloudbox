# The public IP address of your GCP VM
VM_HOST=YOUR_GCP_VM_IP_ADDRESS

# The username to log in with via SSH (e.g., ubuntu, centos, or a custom user)
VM_USERNAME=YOUR_SSH_USERNAME

# The absolute path on your LOCAL machine (where the backend runs) to the SSH private key
# Example for macOS/Linux: /Users/yourname/.ssh/gcp_key
# Example for Windows: C:\Users\yourname\.ssh\gcp_key
VM_PRIVATE_KEY_PATH=PATH_TO_YOUR_SSH_PRIVATE_KEY